<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.layout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Layout">
        <activity android:name=".menuDashboard"></activity>
        <activity android:name=".menuDasboard" />
        <activity android:name=".scrollView" />
        <activity android:name=".tabelLayout" />
        <activity android:name=".frameLayout" />
        <activity android:name=".constraintLayout" />
        <activity android:name=".relativeLayout" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>